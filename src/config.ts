// One source of truth for the product identity.
// The name AND the pitch came from the kickoff vote — one place in the code too.
export const PRODUCT_NAME = "Monotask";
export const PITCH = "One thing at a time, on purpose.";
